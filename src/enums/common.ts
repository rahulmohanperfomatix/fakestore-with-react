export enum FormErrorTypeEnum {
    REQUIRED = "required",
    INVALID = "invalid"
}

export enum FormValidatorsEnum {
    EMAIL = "email",
    TEXT = "text",
    PASSWORD = "password",
    IS_GREATER_NUMBER = "is_greater_number"
}

export enum CustomButtonVariantEnum {
    PRIMARY = "primary",
    SECONDARY = "secondary"
}

export enum LocalStorageEnum {
    AUTH_DETAILS = "auth_details",
}